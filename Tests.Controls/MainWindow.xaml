<Window x:Class="Tests.Controls.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Tests.Controls"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:navi="clr-namespace:Orrest.Navigation;assembly=Orrest.Navigation"
        Title="Tests.Controls"
        Width="800"
        Height="450"
        d:DataContext="{d:DesignInstance local:MainViewModel}"
        mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView Grid.Column="0"
                  BorderBrush="LightGray"
                  ItemsSource="{Binding Path=ViewItems}"
                  SelectedItem="{Binding Path=SelectedView}">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type navi:NavigationItem}">
                    <TextBlock Text="{Binding Path=Title}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Border Grid.Column="1"
                BorderBrush="LightGray"
                BorderThickness="1">
            <ContentControl Content="{Binding ContentView}" />
        </Border>

        <TextBlock Grid.Row="1"
                   Grid.ColumnSpan="2"
                   Text="{Binding Path=ToastMessage}" />
    </Grid>
</Window>