<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--  ── 图标几何数据 ──────────────────────────────────────  -->
    <Geometry x:Key="CheckIconGeometry">M9,18c-.26,0-.51-.1-.71-.29l-5-5c-.39-.39-.39-1.02,0-1.41s1.02-.39,1.41,0l4.29,4.29,10.29-10.29c.39-.39,1.02-.39,1.41,0s.39,1.02,0,1.41l-11,11c-.2.2-.45.29-.71.29Z</Geometry>
    <Geometry x:Key="MinusIconGeometry">M15,2H1c-.55,0-1-.45-1-1S.45,0,1,0h14c.55,0,1,.45,1,1s-.45,1-1,1Z</Geometry>

    <!--  ── CheckBox 隐式样式 ─────────────────────────────────  -->
    <Style TargetType="CheckBox">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
        <Setter Property="Background" Value="{DynamicResource SelectionColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4,0,0,0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel
                        x:Name="Root"
                        Background="Transparent"
                        Orientation="Horizontal">
                        <Border
                            x:Name="Container"
                            Width="18"
                            Height="18"
                            VerticalAlignment="Center"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                            <Grid>
                                <Path
                                    x:Name="CheckMark"
                                    Margin="3"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Data="{StaticResource CheckIconGeometry}"
                                    Fill="{DynamicResource PrimaryForegroundColor}"
                                    Stretch="Uniform"
                                    Visibility="Collapsed" />
                                <Path
                                    x:Name="IndeterminateMark"
                                    Margin="3"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Data="{StaticResource MinusIconGeometry}"
                                    Fill="{DynamicResource PrimaryForegroundColor}"
                                    Stretch="Uniform"
                                    Visibility="Collapsed" />
                            </Grid>
                        </Border>
                        <ContentPresenter
                            x:Name="PART_ContentPresenter"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            RecognizesAccessKey="True" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <!--  悬停时 Container 边框变主题色  -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Container" Property="BorderBrush" Value="{DynamicResource PrimaryColor}" />
                        </Trigger>
                        <!--  选中状态：显示 CheckMark + Container 填充主题色  -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                            <Setter TargetName="Container" Property="Background" Value="{DynamicResource PrimaryColor}" />
                            <Setter TargetName="Container" Property="BorderBrush" Value="{DynamicResource PrimaryColor}" />
                        </Trigger>
                        <!--  不确定状态：显示 IndeterminateMark + Container 填充主题色 75%  -->
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="IndeterminateMark" Property="Visibility" Value="Visible" />
                            <Setter TargetName="Container" Property="Background" Value="{DynamicResource PrimaryColor75}" />
                            <Setter TargetName="Container" Property="BorderBrush" Value="{DynamicResource PrimaryColor75}" />
                        </Trigger>
                        <!--  禁用状态：整体半透明  -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
